<%= form_for(@oracle) do |f| %>
  <% if @oracle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@oracle.errors.count, "error") %> prohibited this oracle from being saved:</h2>

      <ul>
      <% @oracle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
